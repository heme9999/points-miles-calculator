# 已知局限与后续阶段规划 (KNOWN LIMITATIONS & FUTURE SCOPE)

## 一、当前版本已支持的范围
1. 全现金旅行费用 10 大细分项目的完整自付与均摊测算。
2. 航空里程（含税费、加赠、Copay）与酒店积分（含免税/Resort Fee）双重抵扣及费用瀑布。
3. 真实单点价值（CPP）与本地货币兑出价值的精确反算与决策推荐。
4. 纯前端无服务器隐私保护架构，支持本地暂存、打印导出与分享参数。
5. 修复全站估值单位混淆风险，建立标准 `usdCentsPerPoint` 数据体系。
6. 中英文双向 Hreflang 与 104 规范 URL 架构。

---

## 二、当前版本的严格限制 (明确不属于本阶段的功能)

1. **无实时外部 API 依赖**：
   - 不接入 live GDS 机票查询或实时 OTA 酒店库存 API；
   - 避免因第三方接口变动、限流或虚假库存破坏计算器的离线可用性与速度。
2. **不根据目的地自动生成批量伪页面**：
   - 严禁按城市、人数、天数、月份自动排列组合生成成百上千个低质薄内容 SEO 页面；
   - 仅保留高质量、人工推演的代表性案例（美国西海岸与日本）。
3. **无用户账户体系与云端数据库**：
   - 数据保存在用户的浏览器 `localStorage` 中；
   - 清除浏览器缓存会导致本地暂存数据被清除。
4. **不包含实时外汇自动抓取**：
   - 采用标准可调节的默认汇率假设（1 USD = 7.00 CNY），允许用户在需要时微调，防止汇率接口故障导致全站计算中断。
5. **无联盟营销与信用卡推销链接**：
   - 恪守纯粹中立的常客计算工具定位，不插入任何信用卡申请佣金链接（Affiliate Link）或机票推销广告。

---

## 三、待定与后续阶段优化建议

1. **第 2 阶段（数据增强）**：
   - 可在用户主动要求时，提供主流航司标准兑换表格区间参考查询（如按飞行距离的大致里程区间）。
2. **第 3 阶段（导出增强）**：
   - 增加一键生成高美观度长图（Canvas Screenshot）以便用户在常旅客社交群组分享。
