---
layout: base.njk
title: 积分换算现金价值计算器 | 里程与信用卡积分值多少钱
description: 将航空里程和信用卡积分换算为现金价值（美元或人民币）。基于0.6、1.2、1.5、2.0 CPP估值情景测算资产规模与兑现参考。
schemaType: WebApplication
eyebrow: 里程账 · 工具 05
datePublished: "2026-08-14"
dateModified: "2026-08-23"
lastModified: "2026-08-23"
breadcrumbs:
  - name: 计算器
    url: /calculators/
  - name: 积分换算现金价值计算器
---
<h1>积分换算现金价值计算器</h1>
<p class="sub">别把积分当欢乐豆，它们代表真实的消费力。选择估值情景或输入自定义单点价值（CPP），测算手头积分与里程的等价现金估值。</p>

<div class="direct-answer" id="directAnswerBox">
  <strong>直接折现规则：</strong> 按中性航空里程情景 1.5 美分/点测算；人民币金额采用页面显示的汇率假设（1.5美分 = 0.015美元；0.015美元 × <span id="spanFxEcho">7.0</span> = <span id="spanCnyCppEcho">0.105</span>元/点）。例如：持有 50,000 里程约折合 <span id="spanEgCny">¥5,250</span>（或 $750）。该数值仅用于演示，不代表具体积分计划的实时价值。
</div>

<div class="currency-selector" style="margin-bottom: 20px; text-align: right; display: flex; justify-content: flex-end; align-items: center; gap: 10px;">
  <button id="btnShare" class="btn-share">分享此计算</button>
  <div>
    <label for="currency" style="font-weight: bold; margin-right: 8px;">显示货币</label>
    <select id="currency" style="padding: 4px 8px; border-radius: 4px; border: 1px solid #ccc;">
      <option value="CNY" selected>人民币（CNY ¥）</option>
      <option value="USD">美元（USD $）</option>
    </select>
  </div>
</div>
<div id="toast" class="toast">已复制分享链接！</div>

<div class="panel">
  <div class="field">
    <label for="totalPoints">你账户里的总积分/里程<span class="hint" id="hintTotal">点</span></label>
    <input type="number" id="totalPoints" value="150000" min="0" step="1" inputmode="numeric" aria-describedby="hintTotal">
  </div>

  <div class="field" id="fieldFx">
    <label for="fxRate">美元兑人民币汇率假设<span class="hint" id="hintFx">1 USD = ? CNY</span></label>
    <input type="number" id="fxRate" value="7.0" min="0.1" max="20" step="0.01" inputmode="decimal" aria-describedby="hintFx unitFx">
    <div class="unit" id="unitFx">默认汇率仅用于计算演示，请按当日实际汇率调整</div>
  </div>
  
  <div class="field">
    <label for="presetValuation">
      <span>选择估值情景（CPP）<button type="button" class="btn-help-toggle" id="btnHelpScenario" aria-expanded="false" aria-controls="helpScenarioGuide">我该选哪个？</button></span>
      <span class="hint" id="hintValuation">元/点</span>
    </label>

    <div id="helpScenarioGuide" class="help-scenario-guide" style="display: none;">
      <h4>如何选择合适的估值情景（CPP）：</h4>
      <div class="guide-step">
        <strong>1. 有具体兑换计划：</strong>
        <p>优先使用实际兑换项目的真实CPP：<code>实际CPP =（同一行程现金价 − 奖励票税费 − 放弃的现金回馈价值）÷ 所需积分</code>。</p>
        <div class="guide-actions">
          <a href="/calculators/cents-per-point/" class="btn-link-action">使用CPP计算器计算 →</a>
          <a href="/calculators/points-vs-cash/" class="btn-link-action">比较现金与积分 →</a>
        </div>
      </div>
      <div class="guide-step">
        <strong>2. 有历史兑换记录：</strong>
        <p>建议使用最近 3–5 次类似兑换 CPP 的中位数，避免只采用单次极高价值两舱兑换拉高整体预期。</p>
      </div>
      <div class="guide-step">
        <strong>3. 没有具体兑换或历史记录：</strong>
        <p>先使用保守估值情景测算底线，再切换其他情景观察区间。请注意：0.6 不是所有酒店积分的价值；1.5 不是所有航空里程的价值；2.0 也非保证可实现的收益；各计划的航线、舱位、税费与库存都会显著影响实际结果。</p>
      </div>
    </div>

    <select id="presetValuation" aria-describedby="hintValuation unitValuation">
      <option value="custom">自定义CPP</option>
      <option value="0.6">低面值积分情景：0.6美分/点 (约0.042元/点)</option>
      <option value="1.2">保守估值情景：1.2美分/点 (约0.084元/点)</option>
      <option value="1.5" selected>中性航空里程情景：1.5美分/点 (约0.105元/点)</option>
      <option value="2.0">较高价值兑换情景：2.0美分/点 (约0.140元/点)</option>
    </select>
    <input type="number" id="cppValue" value="0.105" min="0.001" step="0.001" style="margin-top:10px;" inputmode="decimal" aria-describedby="hintValuation unitValuation">
    <div class="unit" id="unitValuation">这些CPP仅用于比较不同估值情景，不是官方兑换比例、实时估值或保证价值。最可靠的输入来自您计划兑换项目的实际现金价格、税费和所需积分。</div>
  </div>
</div>

<div class="ticket" aria-live="polite">
  <div class="main">
    <div class="compare">
      <div class="item">
        <div class="k">折合资产估值约</div>
        <div class="v" id="dollarValue" style="word-break: break-word; overflow-wrap: anywhere;">¥0</div>
      </div>
    </div>
    <div class="note" id="explain"></div>
  </div>
</div>

<div class="callout" style="margin-top: 24px;">
  <strong>核心考量：</strong> 长期持有积分可能因计划贬值而降低购买力。较稳妥的做法是围绕明确兑换计划积累积分，避免长期持有不必要的大额余额。
</div>

<div class="article-content prose" style="margin-top: 40px;">
  <h2>估值情景说明表 (Valuation Scenarios)</h2>
  <p>为了帮助您更理性地进行资产模型测算，下表说明了常见预设情景的适用范围与边界：</p>
  
  <div class="responsive-table-wrapper">
    <table class="responsive-table" style="text-align: left;">
      <thead>
        <tr>
          <th style="padding: 12px 14px; font-weight: 600;">估值情景</th>
          <th style="padding: 12px 14px; font-weight: 600;">对应 CPP</th>
          <th style="padding: 12px 14px; font-weight: 600;">典型用途</th>
          <th style="padding: 12px 14px; font-weight: 600;">不代表什么</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="font-weight: 600; color: #0f172a;">低面值积分情景</td>
          <td>0.6¢ / 点</td>
          <td>测试低面值常客积分结果</td>
          <td>不代表所有酒店积分均为此价值</td>
        </tr>
        <tr>
          <td style="font-weight: 600; color: #0f172a;">保守估值情景</td>
          <td>1.2¢ / 点</td>
          <td>无历史数据时的保守底线测算</td>
          <td>不代表官方保底回收价格</td>
        </tr>
        <tr>
          <td style="font-weight: 600; color: #0f172a;">中性航空里程情景</td>
          <td>1.5¢ / 点</td>
          <td>普通航空里程兑换的演示情景</td>
          <td>不代表所有航司统一固定比例</td>
        </tr>
        <tr>
          <td style="font-weight: 600; color: #0f172a;">较高价值兑换情景</td>
          <td>2.0¢ / 点</td>
          <td>测试两舱或甜点航线兑换潜力</td>
          <td>不保证实际必定可以兑出此价值</td>
        </tr>
      </tbody>
    </table>
  </div>

  <h2>常见里程与积分折现参考表</h2>
  <p>下表整理了常见里程数量在不同情景下的参考对照金额（人民币金额按 7.0 演示汇率换算）：</p>
  
  <div class="responsive-table-wrapper">
    <table class="responsive-table">
      <thead>
        <tr>
          <th style="text-align: left;">积分/里程数量</th>
          <th>@ 0.6¢ (约0.042元)</th>
          <th>@ 1.2¢ (约0.084元)</th>
          <th>@ 1.5¢ (约0.105元)</th>
          <th>@ 2.0¢ (约0.140元)</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: left; font-weight: 600; color: #0f172a;">2,000 里程</td>
          <td>¥84 ($12)</td>
          <td>¥168 ($24)</td>
          <td>¥210 ($30)</td>
          <td>¥280 ($40)</td>
        </tr>
        <tr>
          <td style="text-align: left; font-weight: 600; color: #0f172a;">5,000 里程</td>
          <td>¥210 ($30)</td>
          <td>¥420 ($60)</td>
          <td>¥525 ($75)</td>
          <td>¥700 ($100)</td>
        </tr>
        <tr>
          <td style="text-align: left; font-weight: 600; color: #0f172a;">8,000 里程</td>
          <td>¥336 ($48)</td>
          <td>¥672 ($96)</td>
          <td>¥840 ($120)</td>
          <td>¥1,120 ($160)</td>
        </tr>
        <tr>
          <td style="text-align: left; font-weight: 600; color: #0f172a;">17,000 里程</td>
          <td>¥714 ($102)</td>
          <td>¥1,428 ($204)</td>
          <td>¥1,785 ($255)</td>
          <td>¥2,380 ($340)</td>
        </tr>
        <tr>
          <td style="text-align: left; font-weight: 600; color: #0f172a;">50,000 里程</td>
          <td>¥2,100 ($300)</td>
          <td>¥4,200 ($600)</td>
          <td>¥5,250 ($750)</td>
          <td>¥7,000 ($1,000)</td>
        </tr>
        <tr>
          <td style="text-align: left; font-weight: 600; color: #0f172a;">60,000 里程</td>
          <td>¥2,520 ($360)</td>
          <td>¥5,040 ($720)</td>
          <td>¥6,300 ($900)</td>
          <td>¥8,400 ($1,200)</td>
        </tr>
      </tbody>
    </table>
  </div>
  
  <p style="font-size: 0.9em; color: #475569; line-height: 1.5;"><em>注：上述表格为估值情景演示，不代表航司官方保底回收价格。实际兑换价值取决于具体航班的票价水平、税费支出、转点比例及常客计划兑换表。详细估值方法可参阅我们的 <a href="/guides/airline-miles-value/">航空里程价值指南</a> 与 <a href="/methodology/">估值方法论</a>。</em></p>

  <div class="related-tools">
    <h3>更多常旅客决策计算器</h3>
    <ul>
      <li><a href="/calculators/cents-per-point/">单点价值 (CPP) 计算器</a>：精准计算单次机票或酒店兑换扣除税费后的每点实际价值。</li>
      <li><a href="/calculators/points-vs-cash/">积分与现金决策计算器</a>：综合税费与放弃累积的刷卡返利，对比直接买票还是里程换票。</li>
      <li><a href="/calculators/transfer-bonus/">信用卡转点加赠计算器</a>：计算银行加赠活动期间凑齐目标航司里程所需转出的最低积分。</li>
      <li><a href="/calculators/hotel-points-vs-cash/">酒店积分与现金决策计算器</a>：综合住五付四与免度假村费权益评估酒店积分房价值。</li>
    </ul>
  </div>

  <h3>常见问题 (FAQ)</h3>
  <div class="faq">
    <details>
      <summary>如果我的积分即将过期怎么办？</summary>
      <div>
        <p>很多计划（如万豪、英国航空）只要账户有任何余额变动（如赚取或消费积分）即可顺延所有积分的有效期。最简单的办法是通过相关信用卡消费一笔，或者通过购物返利网站（Shopping Portal）赚取少量积分来激活账户。</p>
      </div>
    </details>
    
    <details>
      <summary>我可以把积分卖给别人换成现金吗？</summary>
      <div>
        <p>许多常旅客计划限制或禁止未经授权的积分出售或交换，请以具体计划条款为准。违规交易可能导致账户被封禁、积分清零或机票取消。你可以合法地为亲友兑换机票，但应遵守各计划的使用规范。</p>
      </div>
    </details>

    <details>
      <summary>这个计算器可以直接提现吗？</summary>
      <div>
        <p>本计算器仅提供资产估算功能，不能进行任何实际的金融交易。我们只是帮你弄清楚“你的积分大概值多少钱”。</p>
      </div>
    </details>
  </div>
</div>

<script>
  const $ = id => document.getElementById(id);
  
  // Accessible help drawer toggle
  const btnHelp = $('btnHelpScenario');
  const guideBox = $('helpScenarioGuide');
  if (btnHelp && guideBox) {
    btnHelp.addEventListener('click', () => {
      const isExpanded = btnHelp.getAttribute('aria-expanded') === 'true';
      btnHelp.setAttribute('aria-expanded', !isExpanded);
      guideBox.style.display = isExpanded ? 'none' : 'block';
    });
  }

  const params = new URLSearchParams(window.location.search);
  let currentCurrency = 'CNY';
  const validCurrencies = ['CNY', 'USD'];
  const urlCurr = params.get('currency');
  if (urlCurr && validCurrencies.includes(urlCurr)) {
    currentCurrency = urlCurr;
  } else {
    try {
      const saved = localStorage.getItem('preferredCurrency');
      if (saved && validCurrencies.includes(saved)) {
        currentCurrency = saved;
      }
    } catch(e) {}
  }

  // FX Rate setup
  let defaultFx = 7.0;
  const rawFx = parseFloat(params.get('fx'));
  if (!isNaN(rawFx) && isFinite(rawFx) && rawFx >= 0.1 && rawFx <= 20) {
    defaultFx = rawFx;
  }
  if ($('fxRate')) $('fxRate').value = defaultFx;

  const getActiveFx = () => {
    if (!$('fxRate')) return 7.0;
    const v = parseFloat($('fxRate').value);
    if (!isNaN(v) && isFinite(v) && v >= 0.1 && v <= 20) {
      return v;
    }
    return 7.0;
  };

  const getScenarioUsdCpp = (scenarioKey) => {
    switch(scenarioKey) {
      case '0.6': return 0.6;
      case '1.2': return 1.2;
      case '1.5': return 1.5;
      case '2.0': return 2.0;
      default: return null;
    }
  };

  const updateSelectOptions = () => {
    const fx = getActiveFx();
    const select = $('presetValuation');
    if (!select) return;

    const currentSelected = select.value;

    if (currentCurrency === 'USD') {
      select.options[0].text = 'Custom CPP';
      select.options[1].text = 'Low-value points scenario: 0.6¢ per point';
      select.options[2].text = 'Conservative scenario: 1.2¢ per point';
      select.options[3].text = 'Typical airline-mile scenario: 1.5¢ per point';
      select.options[4].text = 'Higher-value redemption scenario: 2.0¢ per point';
      
      select.options[1].value = '0.6';
      select.options[2].value = '1.2';
      select.options[3].value = '1.5';
      select.options[4].value = '2.0';
    } else {
      const cny06 = (0.6 / 100 * fx).toFixed(3).replace(/\.?0+$/, '');
      const cny12 = (1.2 / 100 * fx).toFixed(3).replace(/\.?0+$/, '');
      const cny15 = (1.5 / 100 * fx).toFixed(3).replace(/\.?0+$/, '');
      const cny20 = (2.0 / 100 * fx).toFixed(3).replace(/\.?0+$/, '');

      select.options[0].text = '自定义CPP';
      select.options[1].text = `低面值积分情景：0.6美分/点 (约${cny06}元/点)`;
      select.options[2].text = `保守估值情景：1.2美分/点 (约${cny12}元/点)`;
      select.options[3].text = `中性航空里程情景：1.5美分/点 (约${cny15}元/点)`;
      select.options[4].text = `较高价值兑换情景：2.0美分/点 (约${cny20}元/点)`;

      select.options[1].value = '0.6';
      select.options[2].value = '1.2';
      select.options[3].value = '1.5';
      select.options[4].value = '2.0';
    }

    if (currentSelected) {
      select.value = currentSelected;
    }
  };

  const updateUnitsAndVisibility = () => {
    const fx = getActiveFx();
    if (currentCurrency === 'USD') {
      if($('hintValuation')) $('hintValuation').textContent = '¢ / 点';
      if($('fieldFx')) $('fieldFx').style.display = 'none';
      if($('spanFxEcho')) $('spanFxEcho').textContent = fx.toString();
      if($('spanCnyCppEcho')) $('spanCnyCppEcho').textContent = (1.5 / 100 * fx).toFixed(3);
      if($('spanEgCny')) $('spanEgCny').textContent = '¥' + (50000 * (1.5 / 100 * fx)).toLocaleString('zh-CN', {maximumFractionDigits:0});
    } else {
      if($('hintValuation')) $('hintValuation').textContent = '元/点';
      if($('fieldFx')) $('fieldFx').style.display = 'block';
      if($('spanFxEcho')) $('spanFxEcho').textContent = fx.toString();
      const cnyRate = (1.5 / 100 * fx).toFixed(3).replace(/\.?0+$/, '');
      if($('spanCnyCppEcho')) $('spanCnyCppEcho').textContent = cnyRate;
      if($('spanEgCny')) $('spanEgCny').textContent = '¥' + (50000 * (1.5 / 100 * fx)).toLocaleString('zh-CN', {maximumFractionDigits:0});
    }
    updateSelectOptions();
  };

  const parseParam = (key, elId, maxVal = 1000000000) => {
    let val = parseFloat(params.get(key));
    if (!isNaN(val) && isFinite(val) && val >= 0 && val <= maxVal) {
      if (key === 'points' && val === 0) return;
      const el = $(elId);
      if (el) el.value = val;
    }
  };
  parseParam('points', 'totalPoints');
  parseParam('totalPoints', 'totalPoints');

  // Scenario parse
  const scenParam = params.get('presetValuation') || params.get('scenario');
  if (scenParam && ['custom', '0.6', '1.2', '1.5', '2.0'].includes(scenParam)) {
    if ($('presetValuation')) $('presetValuation').value = scenParam;
  }

  // Parse valuation parameter
  const rawVal = parseFloat(params.get('valuation') || params.get('cppValue'));
  if (!isNaN(rawVal) && isFinite(rawVal) && rawVal > 0) {
    if ($('cppValue')) $('cppValue').value = rawVal;
  } else {
    // Set default value according to currency
    const activeScen = $('presetValuation') ? $('presetValuation').value : '1.5';
    const fx = getActiveFx();
    if (activeScen !== 'custom') {
      const usdCpp = getScenarioUsdCpp(activeScen) || 1.5;
      if (currentCurrency === 'USD') {
        if ($('cppValue')) $('cppValue').value = usdCpp;
      } else {
        if ($('cppValue')) $('cppValue').value = parseFloat((usdCpp / 100 * fx).toFixed(4));
      }
    }
  }

  if ($('currency')) {
    $('currency').value = currentCurrency;
    $('currency').addEventListener('change', (e) => {
      const oldCurrency = currentCurrency;
      currentCurrency = e.target.value;
      const fx = getActiveFx();
      const select = $('presetValuation');
      const curScen = select ? select.value : 'custom';

      if (curScen !== 'custom') {
        const usdCpp = getScenarioUsdCpp(curScen);
        if (currentCurrency === 'USD') {
          $('cppValue').value = usdCpp;
        } else {
          $('cppValue').value = parseFloat((usdCpp / 100 * fx).toFixed(4));
        }
      } else {
        // Converting custom input accurately without drift
        const oldVal = parseFloat($('cppValue').value) || 0;
        if (oldVal > 0) {
          if (currentCurrency === 'USD' && oldCurrency === 'CNY') {
            $('cppValue').value = parseFloat((oldVal / fx * 100).toFixed(2));
          } else if (currentCurrency === 'CNY' && oldCurrency === 'USD') {
            $('cppValue').value = parseFloat((oldVal / 100 * fx).toFixed(4));
          }
        }
      }

      try { localStorage.setItem('preferredCurrency', currentCurrency); } catch(err) {}
      updateUnitsAndVisibility();
      calcDollars();
    });
  }

  if ($('fxRate')) {
    $('fxRate').addEventListener('input', () => {
      updateUnitsAndVisibility();
      const select = $('presetValuation');
      const curScen = select ? select.value : 'custom';
      const fx = getActiveFx();
      if (curScen !== 'custom' && currentCurrency === 'CNY') {
        const usdCpp = getScenarioUsdCpp(curScen);
        $('cppValue').value = parseFloat((usdCpp / 100 * fx).toFixed(4));
      }
      calcDollars();
    });
  }

  function calcDollars(){
    let P = parseFloat($('totalPoints').value) || 0;
    let V = parseFloat($('cppValue').value) || 0;
    
    const resetState = (msg = '请输入有效的数值。') => {
      $('dollarValue').textContent = '-';
      $('explain').textContent = msg;
      $('dollarValue').classList.remove('win');
    };

    if (P <= 0 || V <= 0 || !$('cppValue').value) {
      resetState(P < 0 || V < 0 ? '输入不能为负数。' : '请输入积分余额和估值。');
      return;
    }
    
    let total = 0;
    if (currentCurrency === 'USD') {
      total = P * (V / 100);
      $('dollarValue').textContent = '$' + total.toLocaleString('en-US', {maximumFractionDigits:2});
      $('explain').textContent = `按 ${V} 美分/点的估值情景计算，你账户里的 ${P.toLocaleString('zh-CN', {maximumFractionDigits:0})} 积分相当于 $${total.toLocaleString('en-US', {maximumFractionDigits:2})} 的测算购买力。该数值为情景演示，非官方保底兑现。`;
    } else {
      total = P * V;
      $('dollarValue').textContent = '¥' + total.toLocaleString('zh-CN', {maximumFractionDigits:0});
      $('explain').textContent = `按 ${V} 元/点的估值情景计算，你账户里的 ${P.toLocaleString('zh-CN', {maximumFractionDigits:0})} 积分相当于 ¥${total.toLocaleString('zh-CN', {maximumFractionDigits:0})} 的测算购买力。该数值为情景演示，非官方保底兑现。`;
    }
    $('dollarValue').classList.add('win');
  }

  $('presetValuation').addEventListener('change', e => {
    const scen = e.target.value;
    const fx = getActiveFx();
    if(scen !== 'custom'){
      const usdCpp = getScenarioUsdCpp(scen);
      if (currentCurrency === 'USD') {
        $('cppValue').value = usdCpp;
      } else {
        $('cppValue').value = parseFloat((usdCpp / 100 * fx).toFixed(4));
      }
      calcDollars();
    }
  });

  $('cppValue').addEventListener('input', () => {
    const val = parseFloat($('cppValue').value) || 0;
    const fx = getActiveFx();
    let matched = 'custom';
    ['0.6', '1.2', '1.5', '2.0'].forEach(s => {
      const usdCpp = getScenarioUsdCpp(s);
      const targetVal = currentCurrency === 'USD' ? usdCpp : parseFloat((usdCpp / 100 * fx).toFixed(4));
      if (Math.abs(val - targetVal) < 0.0001) {
        matched = s;
      }
    });
    $('presetValuation').value = matched;
    calcDollars();
  });

  $('totalPoints').addEventListener('input', calcDollars);
  
  if ($('btnShare')) {
    $('btnShare').addEventListener('click', () => {
      const url = new URL('{{ site.url }}/calculators/points-to-dollars/');
      const p = parseFloat($('totalPoints').value) || 0;
      const v = parseFloat($('cppValue').value) || 0;
      const scen = $('presetValuation').value;
      const fx = getActiveFx();

      if (p > 0) url.searchParams.set('points', p);
      if (v > 0) url.searchParams.set('valuation', v);
      if (scen && scen !== 'custom') url.searchParams.set('scenario', scen);
      if (currentCurrency) url.searchParams.set('currency', currentCurrency);
      if (currentCurrency === 'CNY' && fx !== 7.0) url.searchParams.set('fx', fx);

      navigator.clipboard.writeText(url.toString()).then(() => {
        const toast = $('toast');
        if (toast) {
          toast.style.display = 'block';
          setTimeout(() => { toast.style.display = 'none'; }, 2000);
        }
      });
    });
  }

  updateUnitsAndVisibility();
  if($('cppValue').value) calcDollars();
</script>
