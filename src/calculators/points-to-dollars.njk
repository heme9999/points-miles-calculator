---
layout: base.njk
title: 积分折现计算器 (Points to Dollars) | 算算你的积分净资产
description: 你的百万信用卡积分或航司里程到底值多少钱？输入数量和估值基准，免费帮你换算出相当于多少现金的“积分净资产”。
schemaType: WebApplication
eyebrow: 里程账 · 工具 05
breadcrumbs:
  - name: 计算器
    url: /calculators/
  - name: 积分折现计算器
---
<h1>积分折现计算器 (Points to Dollars)</h1>
<p class="sub">别把积分当欢乐豆，它们是真金白银。输入你账户里的点数和当前市场估值，算算你一共持有多少“无形资产”。</p>

<div class="currency-selector" style="margin-bottom: 20px; text-align: right;">
  <label for="currency" style="font-weight: bold; margin-right: 8px;">显示货币</label>
  <select id="currency" style="padding: 4px 8px; border-radius: 4px; border: 1px solid #ccc;">
    <option value="CNY">人民币（CNY ¥）</option>
    <option value="USD">美元（USD $）</option>
  </select>
</div>

<div class="panel">
  <div class="field">
    <label for="totalPoints">你账户里的总积分/里程<span class="hint" id="hintTotal">点</span></label>
    <input type="number" id="totalPoints" value="150000" min="0" step="1" inputmode="numeric" aria-describedby="hintTotal">
  </div>
  
  <div class="field">
    <label for="presetValuation">当前市场单点估值 (CPP)<span class="hint" id="hintValuation">价值 / 点</span></label>
    <select id="presetValuation" aria-describedby="hintValuation unitValuation">
      <option value="custom">自定义(手动输入)</option>
      <option value="0.12">0.12 (国内航司里程估值示例)</option>
      <option value="0.15">0.15 (Chase Ultimate Rewards 估值示例)</option>
      <option value="0.14">0.14 (Amex Membership Rewards 估值示例)</option>
      <option value="0.05">0.05 (万豪酒店积分估值示例)</option>
    </select>
    <input type="number" id="cppValue" value="0.15" min="0.01" step="0.01" style="margin-top:10px;" inputmode="decimal" aria-describedby="hintValuation unitValuation">
    <div class="unit" id="unitValuation">注：预设数值仅供测算演示，不代表实时价值。美元估值换算人民币受汇率波动影响（以 7.0 概算），请以当日汇率为准。</div>
  </div>
</div>

<div class="ticket" aria-live="polite">
  <div class="main">
    <div class="compare">
      <div class="item">
        <div class="k">折合资产估值约</div>
        <div class="v" id="dollarValue" style="word-break: break-word; overflow-wrap: anywhere;">¥0</div>
      </div>
    </div>
    <div class="note" id="explain"></div>
  </div>
</div>

<div class="callout" style="margin-top: 24px;">
  <strong>核心警告：</strong> 积分不像现金能够稳定保值。各大航司和酒店几乎每年都会进行不同程度的“贬值”（调高兑换门槛或改恶兑换图表）。因此我们强烈建议“Earn and Burn”——赚了就赶紧花，绝对不要把积分当成理财产品囤积。
</div>

<div class="article-content prose" style="margin-top: 40px;">
  <h2>为什么要计算“积分折现价值”？</h2>
  <p>很多常旅客玩家会沉迷于开卡奖励，看着账户里高达几十上百万的积分余额感到满足。然而，这些数字并非银行存款。计算“积分折现价值”（Points to Dollars）主要有两个实际意义：</p>
  <ul>
    <li><strong>清算个人资产</strong>：了解自己手头的“非现金资产”总盘子有多大。很多时候，我们持有的航空里程和酒店积分加起来，价值高达几万甚至十几万人民币。</li>
    <li><strong>衡量持卡成本</strong>：许多高端信用卡（如 Amex Platinum, Chase Sapphire Reserve）每年会收取几千元的高昂年费。如果你账户里的积分折现价值连年费都无法覆盖，或者你根本花不出去，那你其实是在被银行“反薅”。</li>
  </ul>

  <h3>估值是怎么确定的？</h3>
  <p>这个计算器依赖你输入的 **CPP（每点价值）** 参数。你可以随时前往我们的 <a href="/values/">积分估值目录页</a> 查阅各大银行和常客计划的最新市场参考均价。</p>
  <p>如果你已经看中了一张机票，并想知道这次兑换的具体价值，请使用更针对单次出行的 <a href="/calculators/cents-per-point/">CPP 计算器</a>。</p>

  <h2>积分/里程管理的三大铁律</h2>
  <ol>
    <li><strong>Earn and Burn (随赚随花)</strong><br>
    正如上面提示中所说，积分只有在花出去那一刻才真正属于你。银行和航司拥有积分系统的最终解释权，他们可以随时无预警地贬值。把积分留在账户里，购买力只会随着时间流逝而不断缩水。</li>
    <li><strong>灵活性大于一切</strong><br>
    像 <a href="/values/chase-ultimate-rewards/">Chase UR</a> 或 <a href="/values/amex-membership-rewards/">Amex MR</a> 这样可以转给多家不同航空公司的“灵活银行积分”，其价值远高于被锁定在单一航司的里程（如单一的达美航空里程）。在没有明确出行计划前，永远要把积分留在银行端，切忌提前转入航司。</li>
    <li><strong>了解保级与过期规则</strong><br>
    各计划过期规则不同。ANA 和普通 KrisFlyer 里程通常按赚取时间计算有效期（最长36个月）；2020年起赚取的亚洲万里通 (Asia Miles) 则可通过符合条件的赚取或兑换活动自动顺延 18 个月。请务必以各家航司最新官方规则为准，避免积分失效。</li>
  </ol>

  <h3>常见问题 (FAQ)</h3>
  <h3>常见问题 (FAQ)</h3>
  <div class="faq">
    <details>
      <summary>如果我的积分即将过期怎么办？</summary>
      <div>
        <p>很多计划（如万豪、英国航空）只要账户有任何余额变动（如赚取或消费积分）即可顺延所有积分的有效期。最简单的办法是通过相关信用卡消费一笔，或者通过购物返利网站（Shopping Portal）赚取少量积分来激活账户。</p>
      </div>
    </details>
    
    <details>
      <summary>我可以把积分卖给别人换成现金吗？</summary>
      <div>
        <p>绝对不行。几乎所有航空公司和酒店的条款都明确禁止买卖积分。如果被发现，你的账户将被封禁，所有积分清零，且已经出票的机票也会被取消。你可以合法地为亲友出票，但不能涉及金钱交易。</p>
      </div>
    </details>

    <details>
      <summary>这个计算器可以直接提现吗？</summary>
      <div>
        <p>本计算器仅提供资产估算功能，不能进行任何实际的金融交易。我们只是帮你弄清楚“你的积分大概值多少钱”。</p>
      </div>
    </details>
  </div>

  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [{
      "@type": "Question",
      "name": "如果我的积分即将过期怎么办？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "很多计划（如万豪、英国航空）只要账户有任何余额变动（如赚取或消费积分）即可顺延所有积分的有效期。最简单的办法是通过相关信用卡消费一笔，或者通过购物返利网站（Shopping Portal）赚取少量积分来激活账户。"
      }
    }, {
      "@type": "Question",
      "name": "我可以把积分卖给别人换成现金吗？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "绝对不行。几乎所有航空公司和酒店的条款都明确禁止买卖积分。如果被发现，你的账户将被封禁，所有积分清零，且已经出票的机票也会被取消。你可以合法地为亲友出票，但不能涉及金钱交易。"
      }
    }, {
      "@type": "Question",
      "name": "这个计算器可以直接提现吗？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "本计算器仅提供资产估算功能，不能进行任何实际的金融交易。我们只是帮你弄清楚“你的积分大概值多少钱”。"
      }
    }]
  }
  </script>
</div>

<script>
  const $ = id => document.getElementById(id);
  
  const params = new URLSearchParams(window.location.search);
  let currentCurrency = 'CNY';
  const validCurrencies = ['CNY', 'USD'];
  const urlCurr = params.get('currency');
  if (urlCurr && validCurrencies.includes(urlCurr)) {
    currentCurrency = urlCurr;
  } else {
    try {
      const saved = localStorage.getItem('preferredCurrency');
      if (saved && validCurrencies.includes(saved)) {
        currentCurrency = saved;
      }
    } catch(e) {}
  }
  
  const updateUnits = () => {
    if (currentCurrency === 'USD') {
      if($('hintValuation')) $('hintValuation').textContent = '美分/点';
    } else {
      if($('hintValuation')) $('hintValuation').textContent = '元/点';
    }
  };

  if ($('currency')) {
    $('currency').value = currentCurrency;
    $('currency').addEventListener('change', (e) => {
      const oldCurrency = currentCurrency;
      currentCurrency = e.target.value;
      if (oldCurrency !== currentCurrency) {
        $('cppValue').value = '';
        
        const warningMsg = currentCurrency === 'USD' 
          ? '货币已切换。为避免人民币和美元金额混淆，请重新填写个人估值。' 
          : '货币已切换。为避免人民币和美元金额混淆，请重新填写个人估值。';
          
        function clearCalculatorResults() {
          $('dollarValue').textContent = '-';
          $('dollarValue').classList.remove('win');
          $('explain').innerHTML = `<span style="color:#eab308; font-weight:bold;">${warningMsg}</span>`;
        }
        clearCalculatorResults();
      }
      try { localStorage.setItem('preferredCurrency', currentCurrency); } catch(err) {}
      updateUnits();
      if ($('cppValue').value) {
        calcDollars();
      }
    });
  }

  const parseParam = (key, elId, maxVal = 1000000000) => {
    let val = parseFloat(params.get(key));
    if (!isNaN(val) && isFinite(val) && val >= 0 && val <= maxVal) {
      if (key === 'points' && val === 0) return;
      const el = $(elId);
      if (el) el.value = val;
    }
  };
  parseParam('points', 'totalPoints');
  parseParam('valuation', 'cppValue', 1000);

  function calcDollars(){
    let P = parseFloat($('totalPoints').value) || 0;
    let V = parseFloat($('cppValue').value) || 0;
    
    const resetState = (msg = '请输入有效的数值。') => {
      $('dollarValue').textContent = '-';
      $('explain').textContent = msg;
      $('dollarValue').classList.remove('win');
    };

    if (P <= 0 || V <= 0 || !$('cppValue').value) {
      resetState(P < 0 || V < 0 ? '输入不能为负数。' : '请输入积分余额和估值。');
      return;
    }
    
    // If USD, V is in Cents, we divide by 100 to get dollar amount
    let total = 0;
    if (currentCurrency === 'USD') {
      total = P * (V / 100);
      $('dollarValue').textContent = '$' + total.toLocaleString('en-US', {maximumFractionDigits:2});
      $('explain').textContent = `按 ${V} 美分/点的估值计算，你账户里的 ${P.toLocaleString('zh-CN', {maximumFractionDigits:0})} 积分相当于 $${total.toLocaleString('en-US', {maximumFractionDigits:2})} 的购买力。如果一直闲置不用，这笔无形资产将面临缩水风险。`;
    } else {
      total = P * V;
      $('dollarValue').textContent = '¥' + total.toLocaleString('zh-CN', {maximumFractionDigits:0});
      $('explain').textContent = `按 ${V} 元/点的估值计算，你账户里的 ${P.toLocaleString('zh-CN', {maximumFractionDigits:0})} 积分相当于 ${total.toLocaleString('zh-CN', {maximumFractionDigits:0})} 元的购买力。如果一直闲置不用，这笔无形资产将面临缩水风险。`;
    }
    $('dollarValue').classList.add('win');
  }

  $('presetValuation').addEventListener('change', e => {
    if(e.target.value !== 'custom'){
      $('cppValue').value = e.target.value;
      calcDollars();
    }
  });
  ['totalPoints','cppValue'].forEach(id => {
    if($(id)) $(id).addEventListener('input', calcDollars);
  });
  
  updateUnits();
  if($('cppValue').value) calcDollars();
</script>
